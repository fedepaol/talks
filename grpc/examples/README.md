#### Companion examples to the talk "Rpc with steroids with grpc"

To run the examples you'll need to have `make` & `docker-compose` installed.

Just run `make $examplename` to run the example you want.